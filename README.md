شبیه‌ساز و ساده‌ساز گرامر مستقل از متن (CFG Simplifier)

این پروژه یک ابزار تحت وب تعاملی برای ساده‌سازی گرامرهای مستقل از متن (Context-Free Grammars) است. این برنامه تمامی مراحل ساده‌سازی شامل حذف تولیدات تهی، حذف تولیدات واحد و حذف نمادهای بی‌استفاده را به صورت خودکار و مرحله‌به‌مرحله انجام می‌دهد.

✨ ویژگی‌ها

رابط کاربری مدرن: طراحی شیشه‌ای (Glassmorphism) با تم تاریک (Dark Mode) و انیمیشن‌های جذاب.

پردازش سمت کلاینت: تمام محاسبات با جاوااسکریپت خالص در مرورگر انجام می‌شود و نیاز به سرور ندارد.

نمایش مرحله‌به‌مرحله:

حذف تولیدات تهی ($\lambda$-Productions)

حذف تولیدات واحد (Unit Productions)

حذف نمادهای بی‌استفاده (Useless Symbols)

تشخیص هوشمند: اگر گرامر ورودی از قبل ساده باشد، برنامه تشخیص داده و مراحل تکراری را نمایش نمی‌دهد.

مثال‌های تصادفی: قابلیت بارگذاری گرامرهای نمونه متنوع برای تست سریع.

پشتیبانی کامل از فارسی: راست‌چین (RTL) و استفاده از فونت وزیرمتن.

🚀 نحوه اجرا

این پروژه هیچ پیش‌نیازی برای نصب (مانند Node.js یا Python) ندارد.

فایل‌های پروژه را دانلود یا کلون کنید:

git clone https://github.com/your-username/cfg-simplifier.git](https://github.com/bardiaazvanian/CFG-Simplifier.git

فایل .html اصلی (مثلاً index.html) را با مرورگر خود (Chrome, Firefox, Edge) باز کنید.

🛠 تکنولوژی‌های استفاده شده

HTML5: ساختار معنایی صفحه.

Tailwind CSS: فریم‌ورک CSS برای طراحی رابط کاربری واکنش‌گرا و زیبا.

Vanilla JavaScript: پیاده‌سازی منطق و الگوریتم‌های ساده‌سازی گرامر (بدون وابستگی به کتابخانه خارجی).

Vazirmatn Font: فونت فارسی استاندارد و خوانا.

📚 الگوریتم‌ها

این برنامه از الگوریتم‌های استاندارد نظریه زبان‌ها و ماشین‌ها استفاده می‌کند:

۱. حذف $\lambda$-Productions

شناسایی متغیرهای Nullable با روش تکرار نقطه ثابت (Fixed-Point Iteration).

تولید تمام جایگشت‌های ممکن برای قوانین شامل متغیرهای Nullable با استفاده از Bit Manipulation.

۲. حذف Unit Productions

ساخت گراف وابستگی برای تولیدات واحد.

پیمایش گراف (BFS) برای یافتن تمامی متغیرهای قابل دسترس از طریق قوانین واحد.

جایگزینی قوانین واحد با قوانین غیر واحد متغیرهای مقصد.

۳. حذف Useless Symbols

فاز ۱ (Generating): شناسایی متغیرهایی که رشته‌ای از ترمینال‌ها تولید می‌کنند (Bottom-Up).

فاز ۲ (Reachable): شناسایی متغیرهایی که از نماد شروع قابل دسترس هستند (Top-Down با استفاده از BFS).

🤝 مشارکت

اگر پیشنهادی برای بهبود کد یا افزودن ویژگی‌های جدید (مانند تبدیل به فرم نرمال چامسکی) دارید، خوشحال می‌شویم که Pull Request ارسال کنید.
